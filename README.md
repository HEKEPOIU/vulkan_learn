Vulkan Tutorial Odin Implement Version
===

Use [just](https://github.com/casey/just)to make simple command runner, you can use it by
```sh
    # build in debug mode and run.
    just run_debug
    # see more command by following
    just -l
```
